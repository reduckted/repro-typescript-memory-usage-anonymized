import {
  animate,
  state,
  style,
  transition,
  trigger,
} from "@angular/animations";
import { FocusKeyManager, FocusOrigin } from "@angular/cdk/a11y";
import { hasModifierKey } from "@angular/cdk/keycodes";
import {
  AfterContentInit,
  ChangeDetectionStrategy,
  Component,
  ContentChildren,
  EventEmitter,
  Input,
  OnDestroy,
  Output,
  QueryList,
  TemplateRef,
  ViewChild,
} from "@angular/core";
import { Export1531 } from "@workspace/project28";
import { Export2070 } from "@workspace/project104";
import { BehaviorSubject, merge, Observable, Subscription } from "rxjs";
import { startWith, switchMap, takeUntil, tap } from "rxjs/operators";

import { Export2071, Export2072 } from "../../folder1303";

export const Export2073 = 0;

export const Export1628 = 0;

export type Export2074 = 0;
