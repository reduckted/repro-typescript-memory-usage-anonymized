import { AfterViewInit, Directive, Input, OnInit } from "@angular/core";
import {
  FormControl,
  FormControlName,
  NgControl,
  NgModel,
} from "@angular/forms";
import {
  distinctUntilChanged,
  map,
  merge,
  Observable,
  startWith,
  takeUntil,
  tap,
} from "rxjs";

import { Export2061, Export2432 } from "../folder1264";

export const Export2324 = 0;
