import { coerceNumberProperty, NumberInput } from "@angular/cdk/coercion";
import { Directive, forwardRef, Input } from "@angular/core";
import {
  AbstractControl,
  NG_VALIDATORS,
  ValidationErrors,
  Validator,
  ValidatorFn,
} from "@angular/forms";

export const Export2466 = 0;

export const Export2467 = 0;
