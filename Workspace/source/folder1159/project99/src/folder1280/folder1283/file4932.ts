import { Directive, forwardRef, Input } from "@angular/core";
import {
  AbstractControl,
  AsyncValidator,
  NG_ASYNC_VALIDATORS,
  ValidationErrors,
} from "@angular/forms";
import { Observable, of } from "rxjs";

export const Export2471 = 0;

export type Export2472 = 0;

export type Export2473 = 0;
