import { booleanAttribute, Directive, forwardRef, Input } from "@angular/core";
import {
  AbstractControl,
  NG_VALIDATORS,
  ValidationErrors,
  Validator,
} from "@angular/forms";

export const Export2481 = 0;

export const Export2482 = 0;
