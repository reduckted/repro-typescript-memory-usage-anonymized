import { Directive, forwardRef, Input } from "@angular/core";
import {
  AbstractControl,
  NG_VALIDATORS,
  ValidationErrors,
  Validator,
  Validators,
} from "@angular/forms";

export const Export2484 = 0;
